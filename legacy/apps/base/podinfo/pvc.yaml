apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: podinfo-longhorn-pvc
  namespace: podinfo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn